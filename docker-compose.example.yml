# Example docker-compose.yml
# Students should create their own docker-compose.yml file
# This is just a reference - DO NOT use this directly

version: '3.8'

services:
  # PostgreSQL Database Service
  # Students need to configure this properly
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: taskmanager
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

